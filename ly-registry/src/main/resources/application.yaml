server:
  port: 10086

spring:
  application:
    name: ly-registry

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    fetch-registry: false   #是否向注册中心拉取服务
    register-with-eureka: false  #是否把自己注册进注册中心